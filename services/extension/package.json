{
  "name": "@block-lock/extension",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "build": "tsc --noEmit && echo 'Extension build not yet configured'",
    "lint": "tsc --noEmit"
  },
  "dependencies": {
    "@block-lock/shared-types": "workspace:*"
  },
  "devDependencies": {
    "@types/chrome": "^0.0.320",
    "typescript": "^5"
  }
}